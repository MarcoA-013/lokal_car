<%= form_with(model: model) do |form| %>
  <% if model.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(model.errors.count, "error") %> Não foi possível gravar:</h2>

      <ul>
        <% model.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label 'Modelo', style: "display: block" %>
    <%= form.text_field :model %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
